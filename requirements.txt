# 0206 Core Offline Dependencies (Canonical definition in pyproject.toml)
pefile>=2023.2.7
python-docx>=1.1.0
scapy>=2.5.0
capstone>=5.0.0
rich>=13.7.0
pydantic>=2.5.0
cryptography>=41.0.0

# Optional Cloud AI / Enrichment (install via: pip install .[ai] or pip install .[all])
# openai>=1.12.0
# anthropic>=0.18.0
